from .base import *

DEBUG = True
ALLOWED_HOSTS = ['*']

# Local development cache/DB overrides if needed (e.g. SQLite already in base by default)
# Add some dev-only apps
# INSTALLED_APPS += ['debug_toolbar']
# MIDDLEWARE += ['debug_toolbar.middleware.DebugToolbarMiddleware']
